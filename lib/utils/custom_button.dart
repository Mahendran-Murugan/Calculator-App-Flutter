import 'package:flutter/material.dart';

class CustomButton extends StatelessWidget {
  final Colors color;
  final Colors textClor;
  final String btnText;

  const CustomButton({
    required this.color,
    required this.textClor,
    required this.btnText,
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
